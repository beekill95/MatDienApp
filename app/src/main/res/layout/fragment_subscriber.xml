<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.beekill.matdienapp.activities.Admin.SubscriberFragment">

    <!-- TODO: Update blank fragment layout -->

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/subscriberListView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_above="@+id/dataUpdateTextView" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Refresh"
        android:id="@+id/refreshSubscriberButton"
        android:layout_marginTop="25dp"
        android:layout_marginRight="25dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:visibility="invisible" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:text="Small Text"
        android:id="@+id/dataUpdateTextView"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <android.support.design.widget.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/refreshFloatingButton"
        android:layout_alignBottom="@+id/subscriberListView"
        android:layout_alignRight="@+id/refreshSubscriberButton"
        android:layout_alignEnd="@+id/refreshSubscriberButton"
        android:src="@drawable/ic_refresh_black" />

    <android.support.design.widget.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/addSubscriberFloatingButton"
        android:layout_marginBottom="23dp"
        android:layout_above="@+id/refreshFloatingButton"
        android:layout_alignRight="@+id/refreshSubscriberButton"
        android:layout_alignEnd="@+id/refreshSubscriberButton"
        android:src="@drawable/ic_add_black" />

</RelativeLayout>
